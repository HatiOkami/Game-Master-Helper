// get api url from env-config.json in assets
export const environment = {
  production: false,
  apiUrl: '[API_URL]',
  apiVersion: '',
  versions: {
    version: '0',
    revision: '1',
    branch: '0'
  }
};
